{% extends 'base.html.twig' %}

{% block header %}
    {{ include('elements/header.html.twig', {'h1' : page.h1,  'shortHeader' : shortHeader}) }}
{% endblock %}

    {% block header_fixed %}
        {{ include('elements/header-fixed.html.twig') }}
    {% endblock %}

    {% block header_banner %}
        {{ include('elements/header-banner.html.twig',{'h1' : page.h1}) }}
    {% endblock %}

    {% block body %}
        <!--CATALOG PROD-->
        {{ include ('elements/catalog-prod-services.html.twig', {'services': services, 'sectionName': page.h1, 'categoryPath': page.categorySectionId.categoryId.alias})}}

        <!-- ADVANTAGES -->
        {{ include ('elements/advantages.html.twig')}}

        {# SEO TEXT#}
        {{ include ('elements/seo_text.html.twig', {'page': page}) }}

        <!--WORKS EXAMPLES-->
        {{ include ('elements/works-examples.html.twig')}}


        <!--ADVANTAGES-2-->
        {#{{ include ('elements/advantages2.html.twig')}}#}

        <!--PHOTO GALLERY-->
       {# {{ include ('elements/photogallery.html.twig')}}#}
    {% endblock %}
