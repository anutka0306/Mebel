{% extends 'base.html.twig' %}

{% block header %}
    {{ render(controller('App\\Widget\\TopMenu::show')) }}
{% endblock %}

    {% block header_fixed %}
        {{ render(controller('App\\Widget\\TopMenu::showFixed')) }}
    {% endblock %}

    {% block header_banner %}
        {{ include('elements/header-banner.html.twig',{'h1' : page.h1, 'headerImg' : page.headerImg, 'shortHeader' : shortHeader}) }}
    {% endblock %}

    {% block body %}


        <!--ABOUT-->
        {{ include ('elements/about_service.html.twig', {'seoText': page.seoText})}}
        {{ include('elements/popup/two_buttons_block.html.twig') }}


        <!-- ADVANTAGES -->
        {{ include ('elements/advantages.html.twig')}}

        <!--CATALOG PROD-->
        {{ include ('elements/catalog-prod-services.html.twig', {'services': services, 'sectionName': 'Вам может быть интересно', 'categoryPath': page.subcategoryId.categorySectionId.categoryId.alias})}}


        <!--PHOTO GALLERY-->
        {{ include ('elements/photogallery.html.twig')}}
    {% endblock %}
